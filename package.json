{
  "name": "mini-ad-wall",
  "version": "1.0.0",
  "description": "Mini广告墙 - React版本",
  "main": "src/index.jsx",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "echo \"No test specified\" && exit 0",
    "eject": "echo \"This project doesn't support eject\" && exit 0",
    "server": "cd server && npm start",
    "server:dev": "cd server && npm run dev",
    "dev:all": "concurrently \"npm run server:dev\" \"npm run start\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8",
    "concurrently": "^8.2.2"
  }
}

